# MyLittleHarness Product Documentation

This directory is the reusable product documentation spine for MyLittleHarness.
It explains the product architecture and product-facing boundaries without treating any operating root as product authority.

Reusable product docs describe MyLittleHarness serving an explicitly targeted repository directly. The intended chain is `MyLittleHarness -> target repository`.

## Reading Order

1. `architecture/product-architecture.md` - the product architecture thesis, design axioms, and future capability gates.
2. `architecture/layer-model.md` - the layer model that separates authority, lifecycle, projections, adapters, substrate, and product modules.
3. `specs/operating-root.md` - the operating-root and fallback-root boundary.
4. `specs/authority-and-memory.md` - the authority and memory contract.
5. `specs/product-boundary.md` - the boundary between reusable product source and the operating root.
6. `architecture/clean-room-carry-forward.md` - the portable clean-room field manual for an operating project root.
7. `specs/context-and-ceremony-budget.md` - the context-loading and solo-first ceremony contract.
8. `specs/metadata-routing-and-evidence.md` - the metadata, routing, evidence, and doc-gardening contract.
9. `specs/generated-state-and-projections.md` - the generated-state, cache, index, and SQLite projection contract.
10. `specs/generated-state-search-and-sqlite.md` - the search, backlink, repo-map, telemetry, and SQLite projection contract.
11. `specs/attach-repair-status-cli.md` - the small visible CLI target, compatibility command surface, advanced diagnostics, and attach/repair mutation boundary.
12. `reference/command-surface.md` - the product-facing command surface matrix and write-boundary summary.
13. `security.md` - the security, session-helper, runtime-cache, MCP, and "will never do" boundaries.
14. `specs/adapter-boundary.md` - the adapter and optional integration boundary.

## Authority

These docs describe the reusable MyLittleHarness product architecture.
They do not turn the product repository into live workflow memory.

In shipped product behavior, the operating root is the user's target repository after MyLittleHarness is attached.

Legacy evidence is not a default input for fresh product-doc work. Open old material only for a named blocker.

This product tree may contain source, tests, the stdlib build backend, product docs, and minimal compatibility fixtures for CLI/tests.
It must not contain active plans, project memory, research/history/raw intake, archived plans, logs, caches, local databases, generated validation artifacts, or legacy workflow residue.

Public GitHub golden path: source, docs, tests, package metadata, and CI evidence.
The product checkout should show reusable product truth; operating memory belongs in target repositories.
`.mylittleharness/project-workflow.toml` is the neutral workflow manifest for target operating roots, and `.codex/project-workflow.toml` is legacy/client-adapter compatibility, not the core product path.

## Fixture Boundary

The workflow-shaped files under `.codex/`, `.agents/`, and `project/` in this product tree are compatibility fixtures for CLI reports and tests.
They are not the product architecture destination and are not operating memory.

Clean product architecture and specs belong under `docs/...`.

## Current Product Posture

MyLittleHarness is a solo-first, repo-native safety layer for AI-assisted development.
The product architecture keeps human-readable files as authority, uses metadata for routing, treats generated views as rebuildable projections, keeps analyzer reports separate from explicit apply mutation, defines repair snapshots as target-bound safety evidence for existing-content repair, and treats adapters as optional helpers that fail open to repo files.

## V2 Architecture Direction

The accepted v2 direction is to make this file-backed safety layer legible to external agents and orchestrators without turning MyLittleHarness into a swarm runtime. The product should become a deterministic State around the target repository: routes, role profiles, context tiers, human gates, work claims, run evidence, review tokens, approval packets, drift proposals, archives, and dry-run/apply transitions stay repo-visible and source-bound.

The v2 foundation starts with protocol and documentation, not runtime adoption. Route manifests and structured findings come before role profiles. Role profiles and run evidence come before claims and handoffs. Claims, evidence, and review tokens come before worktree or sandbox governance. Notification relays, provider routing, MCP clients, generated indexes, and external orchestrators remain optional adapters; they can help move packets, but they cannot approve lifecycle state.

`manifest --inspect --json` now exposes both `route_manifest` and `role_manifest` protocol data for external orchestrators. The route manifest names orchestration constraints such as `parallelism_class`, `authority_lane`, `claim_scope`, `merge_policy`, `fan_in_gate`, `max_parallelism_hint`, stale-claim policy, and conflict policy. Lifecycle routes stay `sequential_only`, coordinator-owned, and claim-gated; generated caches stay rebuildable and non-authoritative. The role manifest names advisory role permissions, forbidden actions, required outputs, context/output packet requirements, gate classes, human gates, `orchestration_role`, `may_spawn_workers`, `worker_space_boundary`, `isolation_contract`, `fan_in_output_required`, and `coordination_budget`, but it grants no direct apply authority and does not spawn workers.

Product docs should continue rejecting `swarm run` as the first v2 command, hidden daemons, mandatory model-provider gateways, provider credential storage, vector/RAG memory as authority, direct worker writes to shared lifecycle routes, and autonomous release/commit/push behavior.

Product docs should describe optional helpers precisely: MLH core is not daemon-required and does not need a hidden control plane, while hooks, MCP stdio helpers, dashboards, and `mlhd` runtime cache may exist as local session/runtime convenience layers. Those helpers remain advisory and cannot approve lifecycle, roadmap, archive, Git, release, provider, or cache-truth decisions.

The current productization posture is a public GitHub source release at package version `1.0.2`. It is a verified product boundary for the direct `MyLittleHarness -> target repository` model, not a published package-index release, global installation flow, mutating workstation adoption step, or workstation mutation.

Root-level `CHANGELOG.md` and `RELEASE_NOTES.md` carry the `1.0.2` release summary, verification expectations, and owner-approval boundary. They are product documentation, not publication evidence or release approval by themselves.

The target visible CLI is `init`, `check`, `migrate`, `repair`, and `detach`. The current implementation provides primary `init`, already-attached live-root no-op handling, `check`, explicit `migrate --dry-run|--apply` for legacy `.codex/project-workflow.toml` to neutral `.mylittleharness/project-workflow.toml` copies, no-write `detach --dry-run`, marker-only `detach --apply`, and bounded `repair` classes for state frontmatter, docmap metadata, docmap routes, and create-only stable workflow spec fixtures. `check` includes compact check-level drift for docmap gaps, stale root-role wording, primary instruction-surface size warnings, optional `manifest-migrate-available` guidance for legacy-only live roots, explicit delivered-vs-remainder token contradictions, live-root lifecycle route discovery, live `product_source_root` validation, read-only installed-console command-surface lag warnings for advanced lifecycle sentinel commands, read-only Deep Research rubric recovery hints, read-only route metadata warnings for malformed lifecycle frontmatter relationships, relationship hygiene findings under deep/focused hygiene diagnostics, product-source target artifact classification under link diagnostics, report-only grain diagnostics for active-plan slice size, roadmap mapping/hygiene, and archived-plan calibration samples, focused archived-plan context completeness auditing for missing archive targets with root-cause classes and route traces, stale source references, reconstructed evidence, and suspect incomplete-context archives, and focused route-reference inventory with per-class bounded recovery actions plus next safe commands for lifecycle, accepted-work, stale-metadata, compacted-history, optional-history, and generated-cache references; `check --quick` renders the same routine read-only posture as compact text without the full source inventory, while `check --deep` and `check --focus validation|links|context|hygiene|grain|archive-context|route-references|retention|agents` consolidate common read-only diagnostics and deeper section-size details remain advanced diagnostics. Product docs and stable specs are measured as informational context detail rather than routine warning pressure.

It also keeps explicit `bootstrap --package-smoke` package verification, unified `intelligence --query` recovery search with cold archived-plan/reference retrieval and disposable navigation-cache refresh inside `.mylittleharness/generated/projection/`, route-only `intelligence --focus routes`, optional read-only MCP projection access through `adapter` with structured generated-cache posture but no adapter refresh authority, no-write default-active MCP client configuration for `mylittleharness.read_projection` through `adapter --client-config` while repo-visible files remain authoritative, read-only dashboard cockpit/agent-packet output through `dashboard --inspect`, read-only command intent suggestions through hidden `suggest --intent|--list` with JSON output that never executes suggested commands, including broad audit/free-swim routing toward `check`, `intelligence`, hygiene review, and concrete `meta-feedback --dry-run` capture only, Deep Research human-review routing plus rubric-recovery reporting with no prompt generation or model execution, bounded research provenance, distillate intake/validation with quality-status and planning-reliance gates, and reviewed comparison through hidden `research-import`, `research-distill`, and `research-compare`, including `research-compare --archive-sources --repair-links` for reviewed multi-source comparison plus archive-before-removal source cleanup, read-only Git trailer suggestions to `closeout`, source-bound agent run record capture through hidden `evidence --record --dry-run|--apply`, explicit route advice for incoming information through hidden `intake --dry-run|--apply --text "<text>" --target <rel>`, explicit future-idea incubation through hidden `incubate --dry-run|--apply --topic "<topic>" --note "<note>"` or `--note-file <path>` plus `--fix-candidate` for standard MLH debt capture, first-class meta-feedback capture through hidden `meta-feedback --dry-run|--apply` with observed root provenance, configured central product-feedback destination routing through `--to-root` or `MYLITTLEHARNESS_META_FEEDBACK_ROOT` for incubation notes and managed cluster metadata only, signal type/severity, agent-operability micro-friction owner guidance, and local Python dependency-closure gap or ok reports, deterministic active-plan scaffolding and optional narrowed roadmap item association through hidden `plan --dry-run|--apply [--title "<title>"] [--objective "<objective>"] [--roadmap-item <id>] [--only-requested-item]`, bounded accidental activation cleanup through hidden `plan-cancel --dry-run|--apply`, reviewed closeout/archive/next-plan orchestration through hidden `transition --dry-run|--apply`, explicit owner-decision capture through hidden `approval-decision --dry-run|--apply` bound to approval-packet evidence, explicit research/incubation lifecycle cleanup plus read-only relationship scanning, incubation cleanup advisor classifications, text-input audit posture, Entry Coverage diagnostics, terminal `--archive-covered` archival, Deep Research rubric recovery hints, and split suggestions through hidden `memory-hygiene --dry-run|--apply` and `memory-hygiene --dry-run --scan`, explicit accepted-work roadmap item mutation plus reciprocal source-incubation metadata through hidden `roadmap --dry-run|--apply --action add|update --item-id <id>`, atomic accepted-work batch intake through hidden `roadmap --dry-run|--apply --action add-many --items-file <json|yaml>`, explicit closeout/state synchronization, optional active-plan or project-state closeout fact harvesting, structured `product_source_root` recording, optional roadmap/source-incubation closeout sync, coverage-aware incubation auto-archive, completed-plan archiving, and post-writeback plus compact-only operating-memory compaction through hidden `writeback --dry-run|--apply`, compatibility names such as `attach`, `status`, and `validate`, and advanced diagnostics including `doctor`, `preflight`, `preflight --orchestrator-workspace`, `context-budget`, `audit-links`, `suggest`, `intelligence`, `projection`, `semantic`, bare `evidence`, `closeout`, `snapshot`, `adapter`, `bootstrap`, and `tasks`. Those diagnostics remain read-only or explicitly bounded where implemented, but they are progressive-disclosure surfaces rather than the product front door. Top-level help foregrounds `init`, `check`, `migrate`, `repair`, and `detach` and explicitly points to `mylittleharness approval-decision --help` for owner-decision preparation/recording; that route does not waive human authority or approve lifecycle, accepted-work, provider, credential, archive, Git, or release actions. Advanced/compatibility command-specific help and normal report stdout stay available and compatible.

Obsolete evidence cleanup uses the hidden advanced `retention` route, not raw filesystem deletion. `retention scan --path <rel> [--json]` is read-only and reports candidate classification, inbound references, Git tracked/untracked/ignored posture, and expected warning risk. Reviewed `retention retire|tombstone|purge --dry-run|--apply --path <rel> --reason <text>` writes compact receipts under `project/verification/retention-receipts/*.json`; retire also maintains `project/verification/agent-run-retirement-summary.md` so exact historical agent-run records can leave active agent-run validation warnings, including source-hash freshness, malformed metadata, coordination mirror, and root-pair checks, while malformed retirement evidence and unlisted records still warn. Purge refuses referenced or active/current evidence, tombstone preserves a reference target, and all receipts remain evidence only: they cannot approve lifecycle, archive, roadmap, Git, release, or target-repo acceptance.

For local product-source verification, `suggest --intent "run product tests"` points at the repo-visible stdlib test command `python -m unittest discover -s tests` plus nearby focused/check/diff gates. Plan verification discovery uses the same stdlib `unittest` default for Python test layouts and focused `python -m unittest tests.<module>` gates for declared test artifacts, unless repo-visible package scripts, task files, CI, docs, or roadmap verification evidence explicitly name another command. It is advisory verification discovery only and must not invent pytest/package fallbacks, install dependencies, approve release, or move lifecycle state.
For portable adoption handoffs, `suggest --intent "context pack for Deep Research adoption"` adds a pointer-only context-pack section. It names bootstrap files such as `AGENTS.md`, `.mylittleharness/project-workflow.toml`, `project/project-state.md`, optional active-plan and roadmap routes, read-only navigation commands such as `check`, `dashboard --inspect`, and `adapter --client-config --target mcp-read-projection`, plus the reviewed `research-import`/`research-distill` path for external Deep Research output. The context-pack section is deliberately bootstrap pointers only: it does not duplicate authority bodies, call external models, create public repositories, publish, mutate lifecycle, stage, commit, push, release, or approve provider routing.
For durable verification notes, `suggest --intent "verification evidence"` reports the explicit `intake --dry-run --text-file - --target project/verification/<evidence-id>.md` route and the optional `evidence --record --dry-run` agent-run rail instead of asking operators to encode protected route paths inside another `suggest --intent` string. Hook next-safe diagnostics use the same direct route for blocked `project/verification/*.md` shortcut writes, preserving direct-edit blocks while keeping the replacement command executable. When `intake` is given an explicit `project/verification/*.md` target, weak verification/evidence/packet wording can classify the input as verification without requiring colon-form cues; draft verification intake records `status: "partial"` unless the text contains success or failure cues.
For MCP projection adapter inspection, `suggest --intent "inspect projection adapter runtime"` points at `adapter --inspect --target mcp-read-projection` rather than projection-cache rebuilds, keeping runtime/source/root provenance questions on the adapter rail.
`adapter --client-config --target mcp-read-projection` returns the
`mylittleharness.mcp-client-configs.v1` no-write payload for read/propose-only
MCP setup. Its `clientConfigs` map covers generic MCP, VS Code, Claude Code,
and JetBrains AI Assistant profiles, and each profile is copy/review only: no
client files are written, no server is started, no mutating tools or provider
routing are enabled, and no shell passthrough is exposed. The shapes follow the
published client docs for [VS Code MCP configuration](https://code.visualstudio.com/docs/copilot/reference/mcp-configuration),
[Claude Code MCP](https://code.claude.com/docs/en/mcp), and
[JetBrains AI Assistant MCP](https://www.jetbrains.com/help/ai-assistant/mcp.html).
`doctor --integration mcp|vscode|claude-code|jetbrains` is the paired
read-only integration smoke report. It checks install/runtime provenance, root
classification, neutral/legacy manifest posture, MCP stdio startup,
client-profile pointers, native hook posture, generated-cache freshness, and
authority boundaries without writing config, hooks, cache, lifecycle state, Git
state, adapter state, or provider routing. The report names next safe commands
such as `adapter --client-config`, `hooks --doctor`, `hooks adapter --client
claude-code --dry-run`, and projection recovery commands, but those commands
remain suggestions until the operator runs their explicit dry-run/apply routes.

The first-run operator path is deliberately shorter than the full diagnostic surface: run from the product source checkout with `PYTHONPATH=src`, optionally run `bootstrap --package-smoke` against the product checkout, then point `--root` at the target repository and start with `init --dry-run`, `check`, optional legacy-root `migrate --dry-run`, `repair --dry-run`, and `detach --dry-run`. Apply modes stay explicit and target-bound after dry-run review. If a fresh target reports repairable setup findings, run `repair --apply` after reviewing the dry-run and rerun `check` or `check --quick` before calling first contact clean. Successful `init --apply` and compatibility `attach --apply` create the neutral `.mylittleharness/project-workflow.toml` manifest and do not create client-specific hook adapters by default. `migrate --apply` copies an existing legacy `.codex/project-workflow.toml` to the neutral path, preserves the legacy file, and refuses missing legacy files, divergent existing neutral manifests, symlinked targets, and root escapes. project-local Codex native hooks are optional non-authoritative sensors, not correctness prerequisites, and remain installable or refreshable through `hooks adapter --client codex --dry-run|--apply --scope project`; Codex Trust and user-global client configuration remain client-owned. They are not prerequisites for first-run correctness. From an operating-root hook context, MLH commands that only inspect the configured `product_source_root`, such as `check`, `status`, `validate`, `manifest`, `dashboard`, `doctor`, `preflight`, `audit-links`, and `context-budget`, are read-only product-source inspection rather than product mutation. Product-root apply/build/rebuild/cache/server actions, lifecycle writes, and Git mutation still require the owning workflow and remain blocked by hooks. `bootstrap --inspect`, `tasks --inspect`, publication, global installation, PATH/profile edits, user-config mutation, CI, MCP clients, semantic providers, and workstation adoption are not prerequisites for first-run correctness.

Native hook Git diagnostics distinguish the actual command workdir/root from the configured `product_source_root`. Reviewed local-only VCS checkpoints for exact MLH route/evidence files may receive actual-root split-step guidance: stage exact files with `git -C <actual-root> add -- ...`, review `git -C <actual-root> diff --cached --check`, then run the message-file commit as a separate narrow command. Broad staging, generated/runtime caches, reset, clean, amend, push, release, provider routing, and lifecycle authority remain blocked.

The operating-root start pass is portable across agents that can read files and run shell commands. It starts from `AGENTS.md`, `.mylittleharness/project-workflow.toml`, `project/project-state.md`, and the active plan only when `plan_status = "active"` or the user asks about plan/phase/closeout; `.codex/project-workflow.toml` remains the legacy fallback manifest when the neutral manifest is absent. For fuzzy navigation, `dashboard --inspect` is the compact cockpit before deeper intelligence, adapter, or suggest routes. Its JSON payload carries source refs and a `nextLegalDryRun` candidate for route discovery, but that candidate is advisory and cannot approve apply, closeout, archive, roadmap done-status, staging, commit, release, or product-diff acceptance. `meta-feedback` remains opt-in and is not a default start-pass requirement. When a plan is active, `active_phase` and `phase_status` in `project/project-state.md` are the first-class continuation pointers; CLI reports warn when they are missing so agents do not have to infer phase state from recent prose. MLH write rails also refresh marked Current Focus and Memory Routing Roadmap hot-pointer blocks from lifecycle frontmatter when plans open, phases move, plans close, or a reviewed transition composes those steps. Roadmap relationship writes also refresh an existing MLH-owned `Current Posture` section in `project/roadmap.md` from roadmap item metadata when that section is present. `writeback --apply --active-phase <next-phase> --phase-status pending` is the explicit same-plan phase-advance rail: it can mark the previously active phase body done while moving Current Focus to the next pending phase, without approving closeout, archive, roadmap done-status, next-plan opening, staging, or commit. Those managed blocks explicitly label older prose and archived state history as reference context, and live-root `check` reports `state-lifecycle-prose-drift` when current non-historical project-state prose appears to disagree with lifecycle frontmatter. Live-root `status`/`check` summarize whether the active phase still has implementation work pending or whether the next action is explicit closeout/writeback, archive, transition review, or manual commit.

Route discovery is part of the visible lifecycle routing contract: live-root `status`/`check` and `intelligence --focus routes` name the canonical state, active-plan, optional roadmap, incubation, research, stable-spec, decision/do-not-revisit, ADR, verification through the active-plan block and optional `project/verification/*.md` proof/evidence records, closeout/writeback, archive, and docs-routing lanes without turning `AGENTS.md` into a long playbook. `project/roadmap.md` is an optional live-root sequencing surface for accepted work between raw incubation and one bounded active implementation plan; it is read when planning or sequencing requires it, not as mandatory start-path material. The hidden `roadmap --dry-run|--apply` rail is the mechanical write path for that roadmap lane: it requires or safely bootstraps a managed `project/roadmap.md` for add routes, adds canonical `## Items` blocks or updates existing canonical blocks and compatibility top-level `## RM-...` sections from explicit fields, records repeated `--source-member <rel>` source evidence routes, supports repeated `--clear-field <field>` for reviewed first-class metadata cleanup, supports `--action add-many --items-file <json|yaml>` for reviewed JSON or YAML item manifests, validates every batch item before apply, emits per-item dry-run/apply evidence, and writes the final batch to `project/roadmap.md` once in one transaction, refreshes the MLH-owned `Future Execution Slice Queue` from accepted/proposed item metadata, reports accepted/proposed order namespaces separately so duplicate numeric orders across statuses are not treated as sequencing drift, refreshes an existing MLH-owned `Current Posture` section from active/accepted/proposed item metadata, may compact older detailed `done` item blocks with archived-plan and verification evidence into `Archived Completed History`, omits empty strict path relationship fields such as `source_incubation`, `related_incubation`, `source_research`, `source_members`, `related_plan`, and `archived_plan` from rewritten canonical blocks, refuses unsafe roots, malformed roadmaps, duplicate ids, invalid statuses, invalid docs decisions, bad relationship paths, missing related item ids, same-field set/clear conflicts, and malformed batch manifests, and writes only `project/roadmap.md` plus same-transaction source-incubation relationship metadata when a source-incubation route is explicitly claimed. `roadmap normalize --dry-run|--apply` is the same lane's housekeeping operation for physically grouping canonical managed item blocks without changing statuses, source-note relationships, or lifecycle authority.

`plan --roadmap-item` and `writeback --roadmap-item` reuse the same bounded item-block validation to sync explicit plan relationships or same-request closeout summaries without turning roadmap output into approval. That sync accepts canonical `## Items` item blocks and existing compatibility `## RM-...` top-level sections; legacy `slice_members` values that are work-area labels are not treated as covered roadmap item ids. Plan opening may batch the requested roadmap item plus roadmap-slice `covered_roadmap_items`, refresh each explicit source-incubation note so `related_plan` points at `project/implementation-plan.md`, record non-owning `related_incubation` provenance in active-plan metadata without retargeting that source note, `plan --only-requested-item` narrows that opening to one requested item, and archive closeout may batch the requested roadmap item plus active-plan `covered_roadmap_items` in one writeback apply. When `plan --update-active --roadmap-item <id>` replaces an active plan, dry-run/apply reports the before/after source-incubation relationship graph and detaches any replaced active-plan source note that still points at `project/implementation-plan.md`, including stale scalar/list `related_plan` blocks, while leaving archive, roadmap status, promotion, staging, and commit decisions untouched. After an active plan is already archived, `writeback --roadmap-item <id> --archived-plan <project/archive/plans/...>` can refresh same-request closeout facts, the matching roadmap item, and source-incubation relationship metadata without requiring a readable active plan; the archived-plan route supplies the target identity and still cannot approve archive, next-plan opening, staging, commit, or release cleanup. If closeout source-incubation sync points at an already archived `project/archive/reference/incubation/**` note, writeback updates relationship frontmatter in place instead of deriving another archive target or deleting the existing archive note. When a relationship-sync path owns the explicit source note update, it replaces the whole targeted relationship scalar block so stale YAML list continuations under that field are removed instead of leaving malformed frontmatter; this relationship frontmatter block recovery is limited to same-transaction sync and is not general repair authority. Live-root `check`/`validate` also report `route-metadata-*` warnings when routed roadmap, research, incubation, stable-spec, decision, ADR, or verification artifacts carry malformed status, unsafe relationship paths, stale active references, malformed relationship frontmatter, or missing archive targets, and report `active-plan-source-incubation-related-plan-drift` when the active plan names a source note whose `related_plan` points somewhere else; those warnings are advisory only.

Plan, writeback, roadmap, and transition route writes now include a transaction guard for required route references. Dry-run previews newly required lifecycle/archive pointers that would still be unresolved after the planned writes, and apply refuses before mutation unless each required target is safe, concrete, root-relative, and already present or created by the same transaction.

Roadmap-backed plan opening also understands an explicit slice-result gate for audit/diagnostic dependencies. An upstream item can record custom metadata such as `slice_result_gate=decision-packet` and point at a repo-visible decision packet with `slice_result_artifact` or a `project/verification/**` target artifact. Downstream `plan --roadmap-item` refuses until that upstream item is `done` and the packet says `safe_to_continue_existing_sequence: true`; fork fields such as `new_slice_candidates`, `scope_expansions`, or `blocked_followups` keep the downstream item blocked until the roadmap is updated through an explicit dry-run/apply review.

`research-import --dry-run|--apply` now treats decision-packet-shaped imports as structured evidence for reviewability: it reports field-level completeness for `confirmed_fixes`, `new_slice_candidates`, `scope_expansions`, `blocked_followups`, and `safe_to_continue_existing_sequence`, warns when the packet lacks either `safe_to_continue_existing_sequence: true` or non-empty fork fields, and writes a non-shadowing `Decision Packet Field Check` section into the imported artifact. `--source-member <route>` records explicit source-membership metadata and source hashes on new imports; with `--adopt-existing`, the same field repairs a route-visible research artifact that is already present but missing those source-member bindings. The original multiline payload is still preserved under `Imported Research`; for shell-sensitive multiline packets, prefer `--text-file -` or a reviewed UTF-8 file over inline `--text`.

When `roadmap --apply --source-incubation <rel>` sees that the source note is already claimed by another roadmap item, plan, promotion, archive, or implementation relationship, it records the note on the roadmap item as non-owning `related_incubation` provenance and leaves the source note relationship frontmatter unchanged.

`intake --dry-run|--apply` is the hidden route advisor for incoming text before it becomes incubation clutter: dry-run classifies explicit text or UTF-8 `--text-file` input into incubation, research, decision, ADR, verification, docs-impact, archive-reference, or ambiguous lanes without writing files, including future feature/Deep Research prompt-composition ideas that mention decision or docs context only as non-authority background; apply requires a compatible explicit new Markdown `--target` in a live operating root and refuses ambiguous or mismatched destinations while pointing future-idea cases at the safer `incubate --dry-run` fallback. Explicit verification targets can resolve weak verification/evidence/packet wording that would otherwise remain ambiguous, but conflicting strong route cues still have to match the target before apply. Verification intake defaults to `status: "partial"` for draft packets and records `passed` or `failed` only from matching result cues. `incubate --dry-run|--apply` is the hidden mechanical write rail for that incubation lane: it accepts explicit topic/note fields or UTF-8 `--note-file` input for multi-paragraph text, can add `[MLH-Fix-Candidate]` with `--fix-candidate`, creates the canonical directory when safe, creates or appends the same safe-slug note, reports note body line/character/hash identity, and does not decide whether the idea is good or infer missing meaning from arbitrary text. `meta-feedback --dry-run|--apply` wraps that fix-candidate lane with observed-root provenance, configured central product-feedback destination routing, signal type/severity, cluster-aware friction signatures, exact-id and exact-signature dedupe, optional `--dedupe-to <canonical-id>` for intentional near-duplicate appends, recurrence scoring, explicit roadmap-detached reporting, and special `agent-operability-micro-friction` guidance for command ergonomics, route discovery, dry-run/apply wording, docs_decision pressure, and state-transfer hesitation. After a successful `meta-feedback --apply`, the report records whether lifecycle posture was preserved and warns with before/after state when `plan_status`, `active_plan`, phase fields, or the active-plan file posture moved unexpectedly; that warning does not approve closeout, archive, commit, or next-plan movement. The canonical incubation note and managed cluster metadata are written only in the configured central product-feedback live operating root: either pass `--to-root <central-root>`, set `MYLITTLEHARNESS_META_FEEDBACK_ROOT`, or run with `--root` already pointing at that central root. `meta-feedback` does not create or update `project/roadmap.md`; mature clusters need an explicit `roadmap --dry-run|--apply` promotion after review. When `--root` is a serviced/product root, it remains the observed source root when `--from-root` is omitted and must not receive canonical MLH product-debt capture. `plan --dry-run|--apply` is the hidden mechanical write rail for the active-plan lane: it accepts explicit title/objective/task fields or derives missing title/objective/task candidates from the requested `--roadmap-item`, creates the default implementation-plan scaffold, updates lifecycle frontmatter and the Current Focus block, refuses unsafe roots, invalid roadmap links, or active-plan conflicts before writing, and does not infer product architecture or act as an LLM planner. `writeback --from-active-plan` can harvest explicit closeout bullets from active-plan Closeout Summary/Facts/Fields sections before archive closeout, falling back to matching project-state closeout authority when the plan has no explicit section; `writeback --product-source-root` records validated product-source metadata. `transition --dry-run|--apply` is the hidden reviewed orchestration rail for explicit phase completion, active-plan archive, and next-plan opening; next-plan title/objective/task can be derived from `--next-roadmap-item`, dry-run emits scoped review token input digests, apply requires the dry-run review token, and token drift reports current scoped action, closeout, and file digests plus refresh guidance before refusing writes. Roadmap digests participate only when a current or next roadmap item makes roadmap an input. `memory-hygiene --dry-run|--apply` is the hidden mechanical lifecycle cleanup rail for MLH-owned research/incubation artifacts: it accepts explicit source, promoted destination, archive destination, status, Entry Coverage, archive-covered, and exact-link repair fields; `memory-hygiene --dry-run --scan` also classifies active incubation notes as keep-active, archive candidates, entry-coverage-needed, follow-up extraction, exact-link repair candidates, or ambiguous before any cleanup is attempted, and emits reviewable batch-proposal details for covered candidates with stable candidate ids, archive targets, exact link-repair file lists, a proposal token, and per-candidate dry-run/apply command shapes. The scan has no matching apply mode, and the command refuses unsafe roots, product fixtures, path escapes, symlinks, conflicts, and ambiguous source routes before writing; it does not infer semantic promotion meaning. Product-source fixtures do not emit live route-table rows, and route output remains advisory rather than authority for repair, closeout, archive, commit, or lifecycle decisions. `check`, `status`, `validate`, `audit-links`, bare `evidence`, and `closeout` are CLI reports over those files; `evidence --record --dry-run|--apply` is an explicit source-bound record rail, and none of these routes require Codex skills, IDE-native skills, MCP clients, hooks, CI, or workstation adoption.

Acceptance evidence gates now treat declared route artifacts as concrete closeout evidence. If an active plan declares a Markdown `target_artifacts` path under `project/verification/`, `project/research/`, `project/adrs/`, `project/decisions/`, or `project/specs/`, phase completion and archive/done writeback require that artifact to exist and be cited, or require a reviewed artifact substitution that records before/after paths and a residual-risk marker.

These routes do not require Codex skills, IDE-native skills, MCP clients, hooks, CI, or workstation adoption.

Roadmap-backed plan opening now has a visible slice contract. The roadmap side can store advisory `execution_slice`, `slice_goal`, `slice_members`, `slice_dependencies`, `slice_closeout_boundary`, `source_research`, `source_members`, `related_specs`, and `target_artifacts` fields on managed item blocks. The plan side records the executable one-slice contract in active-plan frontmatter with `primary_roadmap_item`, `covered_roadmap_items`, `domain_context`, `target_artifacts`, `execution_policy`, `auto_continue`, `stop_conditions`, and `closeout_boundary`; accepted or active roadmap items with stale no-plan/no-archive boundary prose get a normalized non-authority boundary so explicit plan opening is not confused with lifecycle approval. When explicit roadmap `target_artifacts` are absent, plan synthesis may recover source/test/docs target hints from source-incubation `affected_routes` while excluding lifecycle-only routes such as the active plan and roadmap. Generated plans default to current-phase-only execution with `auto_continue = false`; the operator continues only the active phase named by `project/project-state.md`, records repo-visible evidence/state, and then stops. Plan synthesis also prefers repo-visible verification command discovery from target test artifacts, package scripts, task files, CI workflow commands, documented commands, or roadmap verification summaries; when no concrete command is visible, generated `verification_gates` stay `UNRESOLVED` instead of inventing a universal Python/pytest fallback. When source implementation is verified by target test artifacts, plan synthesis names those adjacent regression tests in phase write scope, and read-only check/grain diagnostics warn when broad pytest gates require unowned regression-test scope. When roadmap `docs_decision` is `updated`, plan synthesis reports `plan-docs-write-scope-impact`, raises phase pressure, and includes docs/spec/package metadata targets or an exact-scope placeholder in the generated docs phase so docs impact is not left to chat-memory widening. Any opt-in auto-continuation must be explicit and must name stop-condition coverage for verification, authority/docs impact, write scope, source reality, sensitive action, and closeout boundaries. These fields help execution and relationship projection, but they do not approve lifecycle, closeout, archive, repair, commit, rollback, or next-slice movement.

When a live operating root records product-source paths in `target_artifacts` or product-source context, link diagnostics classify those references as `product-target-artifact` instead of requiring product files to exist inside the operating root. The in-memory relationship projection uses the parallel `product-target` status for target-artifact edges.

For roadmap-backed plans, MyLittleHarness can also emit a `Plan Synthesis Notes` section plus dry-run/apply findings for bundle rationale, split boundary, target-artifact pressure, and phase pressure. This makes bundle/split decisions inspectable without turning roadmap output into a hard sizing gate or lifecycle decision.

The current install and distribution story is local and bounded. Operators can run from a source checkout with `PYTHONPATH=src`, or verify the package path with `bootstrap --package-smoke`, which performs a temporary no-network install outside the product root and proves metadata, import/version, and the `mylittleharness` console script. `bootstrap --inspect` provides no-write adoption readiness evidence, including interpreter context, package metadata when run on the product checkout, and PATH discovery for the console script. Package-index publication, signed artifacts, global installation, PATH or profile edits, and user-config mutation remain separate future decisions, not prerequisites for normal correctness. Standalone `bootstrap --apply` and standalone workstation mutation are rejected as product surfaces.

Durable evidence direction is Git-native: Git trailers or commit metadata for Git repositories, and repo-visible Markdown closeout fields plus operator summaries for non-git roots. Read-only `closeout` reports may suggest paste-ready Git trailers from explicit closeout fields, and `evidence` reports may assemble source-bound candidates, but neither stages, commits, archives, repairs, approves lifecycle decisions, or creates persistent generated evidence stores.

`writeback --apply --active-phase <next-phase> --phase-status pending` completes the previously active phase body and advances the same active plan to the next pending phase in one lifecycle writeback, while still refusing to treat that movement as closeout, archive, roadmap done-status, next-plan opening, staging, or commit approval.

Archive-active-plan is a reviewed terminal lifecycle close rail: it refuses non-terminal lifecycle state before any archive, roadmap, or source-incubation write unless the same reviewed writeback request supplies a terminal `--phase-status` of `complete`, `blocked`, `deferred`, `abandoned`, or `skipped`. The archived plan keeps synchronized completed `status`/`phase_status` derived copies so an honestly blocked or deferred plan can close without being mislabeled complete.

Docs decisions are explicit closeout facts, not skill-owned side effects. The accepted values are `updated`, `not-needed`, and `uncertain`; `uncertain` keeps closeout language provisional. A docs decision is required when behavior, CLI usage, configuration, setup, contract meaning, permissions, output shape, UX/copy, terminology, rollout, migration, or other user-facing meaning changes. The marked MLH closeout writeback block in `project/project-state.md` is the current closeout fact authority for MLH-owned state writeback. New closeout writeback blocks record plan identity fields such as `plan_id`, `active_plan`, and `archived_plan`; dry-run/apply reports whether existing facts will carry, replace, or refuse before a partial closeout can mix facts from another plan. Read-only `closeout` reports classify mismatched project-state closeout identities as stale and ignore those state facts for field candidates, quality cues, and Git trailer suggestions. Active-plan frontmatter fields and exact active-plan closeout bullets inside explicit Docs Decision, State Transfer, or closeout summary/facts/fields sections are derived diagnostic copies; schema examples and closeout checklists are ignored by closeout sync. Lifecycle-only phase-pointer writeback does not copy prior closeout facts into the active plan. When the same request records only `docs_decision = uncertain` with `phase_status = complete`, writeback may replace mismatched prior closeout facts with the current plan identity and that provisional docs decision so phase-only handoff does not carry stale closeout facts. `writeback --from-active-plan` turns exact active-plan closeout bullets, or complete matching project-state closeout authority facts when the active plan has no explicit closeout section, into same-request closeout facts before preflight, with explicit CLI fields taking precedence over harvested values. `writeback --apply` synchronizes requested derived copies with the state writeback block, maps written `phase_status` to an existing active-plan frontmatter `status` copy, updates the matching active-phase body `- status:` line, records validated `product_source_root` when requested, and refreshes marked Current Focus and Memory Routing Roadmap hot-pointer blocks from lifecycle frontmatter; harvested facts are treated as requested facts after preflight. Optional `--roadmap-item` sync uses only same-request closeout facts plus the active or archived plan path. Lifecycle `phase_status = complete` becomes `done` in the phase body. `writeback --apply --phase-status complete` records a ready-for-closeout boundary and keeps the active plan open; it does not archive the plan, default roadmap items to `done`, archive source incubation, or open the next slice. `writeback --apply --archive-active-plan` is the explicit live-root close rail for the default active plan: it moves the plan to `project/archive/plans/`, clears active lifecycle pointers, records `last_archived_plan`, retires stale `MLH Phase Writeback` tails that would otherwise point at the deleted active-plan route, marks existing `project/verification/*.md` artifacts as pre-archive lifecycle snapshots in the report, and refreshes Current Focus while preserving supplied or harvested closeout facts. `transition --dry-run|--apply` can compose phase completion, active-plan archive, and next-plan opening only after dry-run review; dry-run now previews archive route-write evidence from the projected phase-complete state when phase completion and archive are requested together and records scoped review-token input digests; apply requires the matching review token, skips archive retargeting for the next roadmap item's source-incubation note, and reports current scoped token inputs plus refresh guidance before stopping on token drift without writes, staging, or committing. Successful Work Result capsules for `writeback --apply --archive-active-plan` and reviewed `transition --apply` are step-aware: when the reviewed command already archived the active plan or opened the next plan, the "What remains" text does not list that completed step as still awaiting a manual decision. After successful live-root `writeback --apply`, oversized `project/project-state.md` files above the default 250-line or 25,000-character threshold may be compacted automatically into a reference archive under `project/archive/reference/`; lifecycle-field writebacks that would trigger that auto-compaction refuse before writing unless `--allow-auto-compaction` is supplied after dry-run review or the operator runs `writeback --dry-run|--apply --compact-only` as a separate maintenance step. `writeback --dry-run|--apply --compact-only` runs only that compaction rail without changing closeout facts, lifecycle frontmatter, active-plan copies, or roadmap fields. Dry-run reports whether compaction would run, skip, refuse, or cross the explicit lifecycle auto-compaction boundary, and refusal does not undo a safe state writeback. Successful `writeback --apply` or reviewed `transition --apply` reports keep the lifecycle result separate from a sole compaction-refusal warning and route the remaining work as operating-memory hygiene, not manual trimming, archive, commit, rollback, or next-plan approval. `.agents/docmap.yaml`, `audit-links`, `check`, relevant specs, and observed diff evidence are the portable inputs.

Source-incubation archive blockers (`writeback-incubation-archive-blocked`) after successful `writeback --apply` or reviewed `transition --apply` are nonblocking Work Result hygiene: lifecycle/archive writes still count as completed, while shared or mixed incubation notes stay active until coverage is explicit.

State compaction selection scans the whole `project/project-state.md`: it preserves current authority and the latest relevant update, then archives older/non-current history instead of trimming only the newest note that crossed the size threshold.

The first owned generated-output boundary is `.mylittleharness/generated/projection/`. It may contain rebuildable JSON projection artifacts, a rebuildable `relationships.json` graph derived from repo-visible relationship metadata, and the disposable SQLite FTS/BM25 index, including relaxed plain-text recovery search over source-verified rows; deleting it must not change accepted project truth. `check` and adapter reports describe this as disposable generated projection-cache posture so stale or dirty cache files are not confused with source, lifecycle, or evidence authority. `intelligence --path`, `--full-text`, and `--query` may refresh missing or stale projection artifacts/indexes inside this boundary when navigation needs them, while `check`, `status`, route reports, semantic readiness/evaluation, adapter inspection/client-config/stdio serving, optional preflight templates, snapshot inspection, and bootstrap/package readiness keep their no-write or explicit-boundary contracts. `bootstrap --inspect` is the no-write workstation adoption readiness surface; it can report context but cannot adopt, install, or mutate workstation state. `bootstrap --package-smoke` verifies local install/import/console-script behavior only from temporary locations outside the product source root. Future semantic retrieval runtime, generated semantic output boundaries, generated evidence manifests, additional adapter families, publishing automation, mutation-capable VCS/GitHub behavior, additional hook/CI integrations, and additional mutation procedures require later scoped plans before implementation. Future adoption or publication behavior must use a scoped contract under its own command ownership, not a generic bootstrap apply lane.

`detach --dry-run` reports root posture, preserved authority files, marker target, generated projection posture, manual recovery notes, and boundary reminders without writing files. `detach --apply` creates only `.mylittleharness/detach/disabled.json` in eligible live operating roots, leaves valid existing markers unchanged, treats generated projection artifacts as disposable but preserved, and reports product-source fixtures, fallback/archive or generated-output roots, ambiguous roots, non-default authority paths, unreadable manifest/state surfaces, path conflicts, and invalid marker payloads as fail-closed apply inputs. `disable` is explanatory terminology only, not a command spelling.

The repair snapshot boundary is `.mylittleharness/snapshots/repair/` inside an explicit live operating root. Snapshots preserve pre-repair bytes and metadata for inspection only; they cannot approve repair, closeout, archive, commit, lifecycle decisions. `snapshot --inspect` is the terminal-only no-write inspection surface for snapshot presence, metadata readability, copied-file/hash/path posture, current-target posture, retention, manual rollback instructions, and non-authority wording. `repair --dry-run` reports a no-write `state-frontmatter-repair` snapshot plan only for default-path prose `project/project-state.md`, reports no-write create-only plans for a missing required `AGENTS.md`, missing required `.agents/docmap.yaml`, or missing `project/specs/workflow/*.md` stable spec fixtures, and reports the no-write `.agents/docmap.yaml` route-repair snapshot plan when existing docmap route diagnostics exist. `repair --apply` can snapshot and prepend deterministic state frontmatter before stopping, creates absent required AGENTS contracts, docmaps, and stable spec fixtures without snapshots, and creates a timestamped snapshot before adding missing route entries to an existing docmap. Product fixtures and legacy reference material remain outside state, AGENTS, docmap, stable-spec, and snapshot creation.

Product readiness means the reusable source tree, docs, tests, package metadata, stdlib build backend, and compatibility fixtures are coherent while operating memory and closeout evidence stay in an explicit operating root. The 1.0.x source-release line is the small visible CLI, stdlib package posture, local package-smoke verification, explicit repair/detach mutation boundaries, source-verified intelligence/evidence helpers, optional read-only MCP projection access, disposable generated projections, and gated optional power-ups. The current source-release checklist is documentation-and-verification based: package metadata/runtime version agreement, bytecode-disabled tests, read-only health gates, package smoke from temporary locations outside the product root, product hygiene, and product-boundary wording. Optional power-ups become release surfaces only after their contracts name source-of-truth placement, generated or adapter boundaries, dependency policy, degraded/offline behavior, verification, closeout evidence, and non-authority rules. The stdlib core remains the default dependency posture; semantic runtimes, provider adapters, package extras, publishing helpers, mutating workstation adoption helpers, and other external runtimes require later scoped policy before they become product surfaces. Package-index publication, signed artifact release, global installation, PATH/profile/user-config mutation, and workstation adoption remain outside this source-release correctness; standalone `bootstrap --apply` and standalone workstation mutation are rejected.

For a clean operating-project pilot, `architecture/clean-room-carry-forward.md` carries the reusable invariants and risk register without importing unrelated historical context.
